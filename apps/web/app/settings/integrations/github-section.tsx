'use client';

export function GithubSection() {
  return (
    <p className="text-sm text-[hsl(var(--muted-foreground))]">
      OAuth wiring lands in Phase 3 (when GitHub OAuth app credentials are set on the backend).
    </p>
  );
}
